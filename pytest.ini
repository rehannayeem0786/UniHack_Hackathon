[pytest]
# `pythonpath` keeps `import backend...` working without installing the package
# or scattering sys.path edits through the tests.
pythonpath = .
testpaths = tests
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
